name: _test-judge-control-app-release

on:
  workflow_call:

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-Dwarnings"


jobs:
 test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Cache cargo registry
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/bin/
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
          target/
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
    
    
    - name: Build test
      run: export CODEGEN_SKIP=true && cargo build --release --verbose -p judge_control_app
    - name: Run test
      run: export CODEGEN_SKIP=true && cargo test --verbose
    - name: Lint with clippy
      run: export CODEGEN_SKIP=true && cargo clippy --all-targets --all-features
    - name: Check formatting
      run: export CODEGEN_SKIP=true && cargo fmt --all --check